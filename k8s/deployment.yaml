apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: basic-app
  template:
    metadata:
      labels:
        app: basic-app
    spec:
      containers:
      - name: basic-app
        image: abishekchamlagai/basic-app:latest
        ports:
        - containerPort: 80
